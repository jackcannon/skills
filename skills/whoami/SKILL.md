---
name: whoami
description: >-
  Run `whoami` and return the username. Use for /whoami or "whoami".
---

# whoami

1. Run `whoami` in the shell.
2. Reply with **only** the command's stdout (trimmed). No greeting, explanation, or extra text.
